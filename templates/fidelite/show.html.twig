{% extends 'template.html.twig' %}


{% block body %}
    <h1>Fidelite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ fidelite.id }}</td>
            </tr>
            <tr>
                <th>Valeur</th>
                <td>{{ fidelite.valeur }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_fidelite_index') }}">back to list</a>

    <a href="{{ path('app_fidelite_edit', {'id': fidelite.id}) }}">edit</a>

    {{ include('fidelite/_delete_form.html.twig') }}
{% endblock %}
