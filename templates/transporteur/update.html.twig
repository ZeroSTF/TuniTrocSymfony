{% extends 'template.html.twig' %}

{% block body %}

  <div class="container">
    <div class="row justify-content-center">
      <div class="col-md-8">
        <div class="card">
          <div class="card-header">
            <h1 class="text-center">Edit Transporteur</h1>
          </div>
          <div class="card-body">
            {{ include('transporteur/_form.html.twig', {'button_label': 'Update'}) }}
            <hr>
            <form method="post" action="{{ path('delete_transporteur', {'id': transporteur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
              <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ transporteur.id) }}">
              <button class="btn btn-danger"><i class="fas fa-trash-alt"></i>Delete</button>
            </form>
            <a href="{{ path('app_transporteur') }}" class="btn btn-secondary mt-3"><i class="fas fa-arrow-left"></i> Back to list</a>
          </div>
        </div>
      </div>
    </div>
  </div>
{% endblock %}