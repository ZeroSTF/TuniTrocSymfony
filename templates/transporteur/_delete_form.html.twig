<form method="post" action="{{ path('delete_transporteur', {'id': transporteur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ transporteur.id) }}">
    <button class="btn btn-danger" title="Delete">
       Supprimer  <i class="fa fa-trash"></i>
    </button>
</form>
