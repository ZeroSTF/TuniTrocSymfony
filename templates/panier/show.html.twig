{% extends 'template.html.twig' %}


{% block body %}

<title>Panier</title>
 
    <h1>Panier</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ panier.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ panier.date ? panier.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Transporteurb</th>
                <td>{{ panier.transporteurb ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_panier_index') }}">back to list</a>

    <a href="{{ path('app_panier_edit', {'id': panier.id}) }}">edit</a>

    {{ include('panier/_delete_form.html.twig') }}
{% endblock %}
